import { ChevronRight, Sparkles } from 'lucide-react';
import { InvoiceTable } from '../components/InvoiceTable';
export function OverviewPage({ invoices, onRemind, onAdd }) { const overdue = invoices.filter(invoice => invoice.status === 'overdue').length; return <div className="lower-grid overview-grid"><section className="table-card"><div className="section-head"><div><h2>Recent invoices</h2><p>Saved invoices and reminder status.</p></div></div><InvoiceTable invoices={invoices} onRemind={onRemind} limit={4}/></section><aside className="insight-card"><div className="insight-title"><span><Sparkles size={18}/></span><b>Smart insight</b></div><h3>{overdue ? <>You have <strong>{overdue} overdue invoice{overdue > 1 ? 's' : ''}</strong> to follow up.</> : 'Your payment workspace is ready.'}</h3><p>Keep invoice details complete and send reminders before payments slip.</p><button onClick={onAdd}>Add invoice <ChevronRight size={16}/></button></aside></div>; }
